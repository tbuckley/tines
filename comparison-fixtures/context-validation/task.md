Read `context.md`, then complete these subcases in order. If it names a relevant skill, read that skill only when its conditional subcase begins. Do not read a skill before finalizing routine.

1. `routine`: summarize the supplied global, project, state, and combined briefs; explicitly say unspecified fields are unknown. Preserve every universal rule.
2. `conditional`: give one ordered command/procedure plan for each scope. Include exact scope/workflow bindings, inspection before state movement, Scouting where applicable, a fresh proposal attachment before Re-propose, and no self-approval. An inheriting child uses Root provenance.
3. `old_decision`: state the old human decision and how to retrieve `cmt_old_detail` using the existing JSON command.

Write only JSON to `output.json` with string fields `routine`, `conditional`, and `old_decision`. Do not edit any other file.
