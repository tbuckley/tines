{
	"routine": "Global brief: title is supplied, but its exact text is unknown; owner and acceptance are unknown. Project P brief: title and owner are supplied, but their exact values are unknown; acceptance is unknown. Root state brief: acceptance is supplied, but its exact text is unknown; title and owner are unknown. Combined project P/Root brief: title and acceptance are supplied, but their exact text is unknown; owner is unknown. Every other unspecified field is unknown. Universal rules: preserve human decisions; preserve source conflicts; report unspecified brief fields as unknown; never approve your own proposal.",
	"conditional": "Workflow names/IDs for the four brief scopes, planning issue references, transition action names, and proposal gate details are unspecified and therefore unknown. Resolve them by inspection; Engineering is known only for the supplied Fixture Project's/520 issue and must not be assumed to own Root or project P. In the commands below, $target is the inspected planning issue, $W is its inspected workflow, $W_ROOT is the inspected workflow that owns Root, $scouting_action is the allowed action entering Scouting, and $proposal_slot and $proposal_source come from the inspected proposal gate. These are placeholders, not supplied values. Preserve human decisions and source conflicts, report every unspecified field as unknown, and never approve your own proposal. The supplied issue is terminal in Engineering/Implementation with no available transitions; do not move it or force-set its state. These are plans only.\n\nGlobal: 1. Inspect `tines context list --exact --all-pages --json`; exact scope has no project, state, issue, or label binding and no workflow restriction. 2. Inspect the selected planning issue and `tines workflows show \"$W\" --json` to establish its workflow and allowed transitions; the global brief remains global. If the target is an inheriting child, inspect `tines workflows bases --json` and record Root as the inherited source provenance with its actual owning workflow. 3. Inspect `tines issues show \"$target\" --json` before any state movement. If planning is required, enter Scouting only through its inspected allowed action using `tines issues move \"$target\" \"$scouting_action\"`; if no such action is available, stop that movement. Prepare the proposal with the exact scope and workflow bindings and preserve human decisions, source conflicts, and unknown fields. Before Re-propose, inspect the issue and gate again, attach a fresh proposal version using the gate's exact source form (`tines issues artifacts attach \"$target\" \"$proposal_slot\" \"$proposal_source\"`), verify its freshness and requirements with `tines issues show \"$target\" --json`, then use `tines issues move \"$target\" 'Re-propose'` only if that exact action is offered. Leave approval to a separate authorized human; never self-approve.\n\nProject P: 1. Inspect `tines projects show 'P' --json` and `tines context list --exact --project 'P' --all-pages --json`; exact scope is project P only, with state, issue, and label unset and no state/workflow restriction added to the brief. 2. Inspect the selected issue in P and `tines workflows show \"$W\" --json` to establish the actual target workflow. If it is an inheriting child, inspect `tines workflows bases --json` and retain Root provenance with its owning workflow. 3. Inspect `tines issues show \"$target\" --json` before any state movement. If planning is required, enter Scouting only through its inspected allowed action using `tines issues move \"$target\" \"$scouting_action\"`; if no such action is available, stop that movement. Prepare the proposal with the exact scope and workflow bindings and preserve human decisions, source conflicts, and unknown fields. Before Re-propose, inspect the issue and gate again, attach a fresh proposal version using the gate's exact source form (`tines issues artifacts attach \"$target\" \"$proposal_slot\" \"$proposal_source\"`), verify its freshness and requirements with `tines issues show \"$target\" --json`, then use `tines issues move \"$target\" 'Re-propose'` only if that exact action is offered. Leave approval to a separate authorized human; never self-approve.\n\nRoot state: 1. Inspect `tines workflows bases --json` and `tines workflows show \"$W_ROOT\" --json` to resolve the owning workflow and inheritance, then `tines context list --exact --state \"$W_ROOT/Root\" --all-pages --json`. Exact scope is state $W_ROOT/Root only; project, issue, and label are unset. 2. Inspect the selected target issue and `tines workflows show \"$W\" --json` before movement. For an inheriting child, keep the brief bound to $W_ROOT/Root and attribute inherited material to Root; separately record the child's actual workflow/state as the execution target. 3. Inspect `tines issues show \"$target\" --json` before any state movement. If planning is required, enter Scouting only through its inspected allowed action using `tines issues move \"$target\" \"$scouting_action\"`; if no such action is available, stop that movement. Prepare the proposal with the exact scope and workflow bindings and preserve human decisions, source conflicts, and unknown fields. Before Re-propose, inspect the issue and gate again, attach a fresh proposal version using the gate's exact source form (`tines issues artifacts attach \"$target\" \"$proposal_slot\" \"$proposal_source\"`), verify its freshness and requirements with `tines issues show \"$target\" --json`, then use `tines issues move \"$target\" 'Re-propose'` only if that exact action is offered. Leave approval to a separate authorized human; never self-approve.\n\nProject P + Root: 1. Inspect `tines projects show 'P' --json`, `tines workflows bases --json`, and `tines workflows show \"$W_ROOT\" --json`, then `tines context list --exact --project 'P' --state \"$W_ROOT/Root\" --all-pages --json`. Exact scope is the conjunction of project P and state $W_ROOT/Root; issue and label are unset. 2. Inspect the target issue in P and `tines workflows show \"$W\" --json`. For an inheriting child, preserve the P + $W_ROOT/Root binding and Root provenance while separately identifying the child's actual workflow/state. 3. Inspect `tines issues show \"$target\" --json` before any state movement. If planning is required, enter Scouting only through its inspected allowed action using `tines issues move \"$target\" \"$scouting_action\"`; if no such action is available, stop that movement. Prepare the proposal with the exact scope and workflow bindings and preserve human decisions, source conflicts, and unknown fields. Before Re-propose, inspect the issue and gate again, attach a fresh proposal version using the gate's exact source form (`tines issues artifacts attach \"$target\" \"$proposal_slot\" \"$proposal_source\"`), verify its freshness and requirements with `tines issues show \"$target\" --json`, then use `tines issues move \"$target\" 'Re-propose'` only if that exact action is offered. Leave approval to a separate authorized human; never self-approve.",
	"old_decision": "The old human decision is: keep exact current bodies available. Retrieve the exact stored body of cmt_old_detail with the existing JSON command:\ntines issues show Fixture/520 --json | jq -er --arg id cmt_old_detail 'first(.comments[] | select(.id == $id) | .body) // error(\"comment not found: \\($id)\")'\nIts supplied body says to use the existing JSON command. The supplied retrieval reference is Fixture/520, whereas the issue heading is Fixture Project's/520 and comment provenance also names Fixture Project/520; preserve these source differences rather than silently replacing the provided retrieval reference."
}
