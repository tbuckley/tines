# Root state guidance

Always verify acceptance before leaving this state.

On rework only: read the rejection, update the design, attach a fresh design, and use Re-propose. A child inheriting Root uses the Root workflow binding. Never self-approve.
